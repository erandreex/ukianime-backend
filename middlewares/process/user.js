const field = require("../fields");

const editar = [field.firstname, field.lastname];

const respuesta = [field.answer];

const requirePassword = [field.requirePassword];

module.exports = {
	editar,
	respuesta,
	requirePassword,
};
